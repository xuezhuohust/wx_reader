<view wx:if="{{book}}" class="page-shell chat-page">
  <!-- Header -->
  <view class="chat-header">
    <view class="nav-back" bindtap="handleBack">
      <view class="back-arrow"></view>
    </view>
    <view class="header-book-info">
      <text class="header-title">{{book.title}}</text>
      <text class="header-author">{{book.author}}</text>
    </view>
  </view>

  <!-- Messages -->
  <scroll-view
    class="chat-body"
    scroll-y
    enhanced
    scroll-with-animation
    scroll-into-view="{{scrollIntoView}}"
    enable-back-to-top
  >
    <view class="message-list">
      <view class="welcome-section">
        <view class="welcome-icon">AI</view>
        <text class="welcome-title">与《{{book.title}}》对话</text>
        <text class="welcome-desc">我是您的智能阅读助手，您可以针对书中的内容向我提问。</text>
      </view>

      <view
        wx:for="{{messages}}"
        wx:key="id"
        id="{{item.id}}"
        class="message-row {{item.role === 'user' ? 'row-user' : 'row-ai'}}"
      >
        <view class="message-bubble {{item.role === 'user' ? 'bubble-user' : 'bubble-ai'}}">
          <view class="message-text">
            <text user-select>{{item.content}}</text>
            <view wx:if="{{item.loading}}" class="typing-indicator">
              <view class="dot"></view><view class="dot"></view><view class="dot"></view>
            </view>
          </view>
          
          <view
            wx:if="{{item.role === 'ai' && !item.loading}}"
            class="voice-btn"
            data-id="{{item.id}}"
            data-content="{{item.content}}"
            bindtap="handleSpeakMessage"
          >
            <view class="voice-icon {{playingMessageId === item.id ? 'playing' : ''}}"></view>
            <text>{{playingMessageId === item.id ? '正在播放' : '播放语音'}}</text>
          </view>
        </view>
      </view>
      
      <view class="bottom-space" style="height: {{keyboardHeight + 200}}rpx;"></view>
    </view>
  </scroll-view>

  <!-- Input Area -->
  <view class="chat-footer" style="bottom: {{keyboardHeight}}px;">
    <view class="quick-tips" wx:if="{{!inputValue && keyboardHeight === 0}}">
      <scroll-view scroll-x enhanced show-scrollbar="{{false}}">
        <view class="tips-row">
          <view
            wx:for="{{quickQuestions}}"
            wx:key="*this"
            class="tip-pill"
            data-question="{{item}}"
            bindtap="handleQuickQuestion"
          >
            {{item}}
          </view>
        </view>
      </scroll-view>
    </view>

    <view class="input-panel">
      <textarea
        class="message-input"
        placeholder="向 AI 提问..."
        placeholder-style="color:var(--stone);"
        value="{{inputValue}}"
        bindinput="handleInput"
        bindconfirm="handleSend"
        confirm-type="send"
        auto-height
        adjust-position="{{false}}"
        maxlength="1000"
      />
      <view class="send-action {{inputValue ? 'active' : ''}}" bindtap="handleSend">
        <view class="send-icon"></view>
      </view>
    </view>
  </view>
</view>
